{
  "scripts": {
    "build": "node build.js",
    "dev": "WIDGET_URL=http://localhost:3000 BASE_URL=http://localhost:8788 npm run build && npx wrangler pages dev dist --port 8788"
  },
  "dependencies": {
    "esbuild": "^0.18.11"
  },
  "devDependencies": {
    "wrangler": "^4.59.3"
  }
}